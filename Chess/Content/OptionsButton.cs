using Microsoft.Xna.Framework.Input;
using System;

namespace Chess.Content
{
    internal class OptionsButton
    {
        public OptionButtonState MarkedState { get; internal set; }

        internal void Update(MouseState current, Input previous)
        {
            throw new NotImplementedException();
        }
    }
}